import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('welcome'),
        ),
        body: Center(
          child: Text('this is app'),
        ),
      ),
    ),
  );
}
